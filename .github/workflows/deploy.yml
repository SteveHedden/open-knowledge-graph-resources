name: Deploy Catalog Site

on:
  push:
    branches:
      - main
    paths:
      - "site/**"
      - "data/**"
      - "curation/**"
      - "vocabularies/**"
      - "ontology.ttl"
      - "sources.ttl"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Build Pages artifact
        run: |
          mkdir -p _site/data _site/curation _site/vocabularies
          cp -R site/. _site/
          cp -R data/. _site/data/
          cp -R curation/. _site/curation/
          cp -R vocabularies/. _site/vocabularies/
          cp ontology.ttl _site/ontology.ttl
          cp sources.ttl _site/sources.ttl

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
